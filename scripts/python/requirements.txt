requests>=2.28.0
beautifulsoup4>=4.12.0

# Credentials are loaded from environment variables or a local .env file by
# wp_config.py (which has a built-in .env parser — no extra dependency needed).
# Copy .env.example to .env and fill in your WordPress application password.
